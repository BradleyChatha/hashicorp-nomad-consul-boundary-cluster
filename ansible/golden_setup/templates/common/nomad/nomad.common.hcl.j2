acl {
    enabled = true
}

addresses {
    rpc = "{{ '{{' }} GetPrivateIP {{ '}}' }}"
}

datacenter          = "{{ v_nomad_datacenter }}"
data_dir            = "/opt/nomad/data"
leave_on_interrupt  = true
leave_on_terminate  = true
region              = "{{ v_nomad_region }}"