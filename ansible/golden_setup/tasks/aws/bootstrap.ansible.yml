---
- name: Wait for connection
  ansible.builtin.wait_for_connection:

- name: Bootstrap Consul
  ansible.builtin.include_tasks: bootstrap.consul.ansible.yml

- name: Bootstrap Nomad
  ansible.builtin.include_tasks: bootstrap.nomad.ansible.yml
